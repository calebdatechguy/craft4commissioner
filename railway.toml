[build]
builder = "nixpacks"
buildCommand = "bun install && bun run build-frontend"

[deploy]
startCommand = "bun run prod.ts"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
